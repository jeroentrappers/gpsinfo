<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.GPSinfo" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="Theme.GPSinfo.Splash" parent="Theme.SplashScreen">
        <!-- Navy that matches the launcher icon background, so the splash
             visually continues into the launcher artwork. -->
        <item name="windowSplashScreenBackground">#0A0E29</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.GPSinfo</item>
    </style>
</resources>
