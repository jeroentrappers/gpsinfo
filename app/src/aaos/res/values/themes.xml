<?xml version="1.0" encoding="utf-8"?>
<!-- Debug/AAOS-only: edge-to-edge window for the car app activity so
     the map surface extends under the system bars, Waze-style. The
     bars stay host-drawn; they just stop being opaque walls. -->
<resources>
    <style name="CarAppEdgeToEdge" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
